<div class="col-md-8">
  <div class="card">
    <div class="card-header p-2">
      <ul class="nav nav-pills">
        <li class="nav-item"><a class="nav-link active" href="#timeline" data-toggle="tab">Lịch Sử Giới Thiệu</a></li>
      </ul>
    </div>

    <div class="card-body">
      <div class="tab-content">
        <div class="tab-pane active" id="timeline">
          <div class="timeline timeline-inverse">

            <% if orders.size == 0 %>
              Chưa có đơn nào
            <% else %>
        <% orders.each do |o| %>

        <!-- /.tab-pane -->

              <div class="time-label">
                <span class="bg-danger"><%= l(o.created_at,format: :custom) %></span>
              </div>

              <div>
                <div class="timeline-item">
              <h3 class="timeline-header border-0">
              <span class="text-bold text-blue"> Tổng Giá: <%=  o.total.to_s %></span> <ul>
                  <% if o.discount_unit.equal? 'percentage' %>
                  <li>Giảm Giá theo %: <b><%=  o.discount_value.to_s %></b></li>

                <% elsif o.discount_unit.equal? 'by_price' %>
                  <li>Giảm Giá: <b><%=  o.discount_value.to_s %></b></li>
                  <% else %>
                  <li>Không Giảm Giá</li>

              <%end  %>


              <li>Phí ship: <b><%=  o.shipping_cost.to_s %></b></li>
              <li>Cập Nhật Lần Cuối: <%= l(o.updated_at,format: :custom) %></li></ul>
              </h3>
                </div>
              </div>
            <% end %>
          <div><i class="far fa-clock bg-gray"></i></div>
            <% end %>

          </div>
      </div>
    </div>
  </div>
</div>
</div>